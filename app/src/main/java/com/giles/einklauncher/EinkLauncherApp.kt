package com.giles.einklauncher

import android.app.Application

/** Application entry point. Kept minimal — repositories are lightweight and constructed lazily. */
class EinkLauncherApp : Application()
